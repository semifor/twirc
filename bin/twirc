#!/usr/bin/perl
use warnings;
use strict;
use Log::Log4perl qw/:easy :levels/;
use POE qw(Component::TSTP);
use YAML;
use Net::Twitter::IRC;

my $app = Net::Twitter::IRC->new_with_options();

POE::Kernel->run;
